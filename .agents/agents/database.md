# Agent Protocol: Database Architect Agent

## Responsibilities
- Design versioned PostgreSQL SQL migration scripts under `supabase/migrations/`.
- Ensure Row Level Security (RLS) is enabled and verified on all public schemas.
- Optimize database indexes (`btree`, `gin`, `brin`, `pg_trgm`) for low-latency queries.
- Audit double-entry ledger constraints and geographic reference tables.
